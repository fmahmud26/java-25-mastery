# Rapid-fire: Core Java

Answer each in ≤90s.

1. Why mutable keys break HashMap?  
2. When is `Optional` as a field wrong?  
3. try-with-resources vs manual finally — why?  
4. Record vs class for a JPA entity?  
5. `==` vs `equals` for Integer 128?  
6. Checked vs unchecked — when custom checked?  
7. Why string concat in loops hurts?  
8. What does `final` on a field buy for publication?  
9. Fail-fast iterator — what does CME mean?  
10. Why not `intern()` request IDs?  

After: peek bank only for misses — re-answer aloud.
